* 기본제공 타입의 객체는 직접 손으로 초기화. 경우에 따라 저절로 되기도 하고 안되기도 하기 때문

* 생성자에서는, 데이터 멤버에 대한 대입문을 생성자 본문 내부에 넣는 방법으로 멤버를 초기화하지 말고 멤버 초기화 리스트를 즐겨 사용. 그리고 초기화 리스트에 데이터 멤버를 나열할 때는 클래스에 각 데이터 멤버가 선언된 순서와 똑같이 나열

* 여러 번역 단위에 있는 비지역 정적 객체들의 초기화 순서 문제는 피해서 설계해야 함. 비지역 정적 객체를 지역 정적 객체로 바꾸면 됨.